{
  "name": "pixiv-nai-gallery-tooling",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1",
    "verify:quick": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1 -SkipBrowserTests",
    "probe:char-swap": "node scripts/probe_char_swap_ui.js",
    "workspace:build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "playwright": "1.60.0"
  },
  "dependencies": {
    "l2d-widget": "0.1.0"
  }
}
