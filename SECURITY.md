# Security Policy

## Supported versions

安全修复优先面向升级版仓库最新的 `main`、稳定版 `h1neolzr7f/NaiXueZhang-Studio` 的 `main`，以及最新官方 Release。较旧版本可能只获得升级建议。

## 报告安全漏洞

请不要在公开 Issue、截图或日志中披露 Token、Cookie、私人素材、家庭目录、完整利用代码或其他敏感信息。

优先使用仓库 Security 标签页中的 **Private vulnerability reporting**。若该入口暂不可用，可以创建一个不含漏洞细节的 Issue，仅说明“需要私密安全联系方式”，维护者会提供后续渠道。

报告建议包含：

- 受影响版本或 Commit；
- 最小复现步骤；
- 影响范围和攻击前提；
- 是否涉及凭据、文件写入、更新链路或远程请求；
- 可行的缓解或修复建议；
- 适合公开披露的时间安排。

维护者收到有效报告后会确认问题、评估影响并跟进修复。请在公开披露前给予合理修复时间。

## 重点安全边界

项目尤其关注：

- NovelAI Token、Pixiv refresh token、Cookie 和 API Key 明文落盘；
- localhost API 被其他网页或本地进程滥用；
- 路径遍历、任意文件读取/删除/覆盖；
- 更新清单、下载包和安装器供应链；
- 发布流程绕过后处理或人工边界；
- 日志、发行包、测试数据和 Git 历史中的隐私泄露；
- AI Butler 工具权限、任意 IO 和静默发布。

## 当前保护措施

- 服务默认只监听 `127.0.0.1`；
- 写操作要求每次启动随机生成的 `X-Session-Token`；
- `/api/session-token` 仅对回环客户端（`127.0.0.1`/`::1`）发放令牌：即使设置 `GALLERY_ALLOW_REMOTE=1` 监听局域网，远程客户端也只能读取、无法获取写操作令牌（返回 403）；
- CORS 仅允许本机可信 Origin；
- 通过 `GALLERY_HOST` 覆盖监听地址为非 loopback 时，必须同时设置 `GALLERY_ALLOW_REMOTE=1` 明确确认，否则拒绝启动；
- NovelAI Token 与 Pixiv refresh token 在 Windows 上通过 DPAPI 加密落盘；非 Windows 无法加密时拒绝写入 `data/`，不会改称已加密却保存明文；
- 主文件、备份和临时文件有明文凭据回归测试；
- 更新清单和更新包必须使用 HTTPS，SHA-256 必填并校验；
- 图片和本地素材路径执行允许根目录检查；
- 作者清理先移动到 `data/_trash/`，避免不可恢复误删；
- 发布包排除数据库、图库、Cookie、凭据、缓存和本地日志；
- 付费生图走持久化任务：HTTP 5xx 有响应时不自动重试，崩溃恢复标记扣费未知；
- 会话令牌拉取失败时写请求 fail-closed，401/403 只刷新一次；
- 生成库队列、运营页和换角灯箱对不可信字符串转义，不再把 API `message` 直接写入 `innerHTML`；
- 小镜三条车道分离：解答只读、检修只跑具名剧本、生产必须亮票；检修不会改系统代理、不会自动拉起采集；
- 主图库为空时，启动/配置采集和 `modify_setting` 启用采集都会被拒绝；
- 小镜自动模式配置写入 `data_dir()` 下的 `butler_auto.json`，不再写死项目根 `data/`；
- 生成库启动维护失败会打印 WARNING，不再静默吞掉。

## 凭据泄露处置

怀疑凭据泄露时：

1. 立即在 NovelAI、pixiv 或相关服务侧吊销并重新签发凭据；
2. 停止运行可能受影响的非官方版本；
3. 删除泄露位置的副本、日志和压缩包；
4. 更新到最新官方版本；
5. 确认本地 `.local.json` 中敏感字段为 `dpapi:v1:` 密文；
6. 检查 Git 历史和 Release 附件，必要时进行历史清理和重新发布。

## 本机信任模型（务必阅读）

本产品是**单用户本机工作台**，不包含真实的用户认证体系：

- `/api/session-token` 对本机任何进程开放（但只对回环客户端发放，远程客户端一律 403）；`X-Session-Token` 只防御浏览器跨站请求，**不防御**同机其他用户或本地恶意进程；
- 能访问 `127.0.0.1:8797` 的本机进程即拥有完整管理权限（Token 池、生成图删除、爬虫启停、维护清理等）；
- 因此请勿在多用户共享机器上以他人可访问的方式运行本服务，也**不要**将监听地址改为 `0.0.0.0` 或局域网地址——这会让同网段任何人获得同等权限。

## 范围外事项

以下通常不作为官方安全漏洞处理：

- 仅在用户主动关闭安全措施后发生的问题；
- 第三方 Fork 或重新编译版本自行加入的行为；
- 不包含安全影响的第三方接口兼容性故障；
- 要求绕过验证码、限流、账号限制或平台访问控制的请求。

---

_最后更新：2026-08-12_
