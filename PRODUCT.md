# Pixiv NAI Gallery 产品战略

## 定位
本地优先的 NovelAI 专属图库与再创作工作台：从 Pixiv 发现候选，NAI 元数据验证入库，
到标签复用、Studio 再创作、后处理与 Pixiv 发布复盘，形成一条可追溯的创作产线。

升级版 v1.5.0 与稳定版一样默认打开经典图库首页；`/app` 工作区仍可用。

## 核心闭环
采集/镜像 → 入库/索引 → 浏览/搜索/收藏 → Prompt/标签复用 → NAI 再创作 → 后处理 → Pixiv 发布与复盘。

## 目标用户
高频使用 Pixiv、aitag、NovelAI 和本地 AI 图像素材的个人创作者。

## 北极星指标
- 每周被复用进入 Studio 的图库作品数。
- 生成图中带完整来源链路和 meta 的比例。
- 从找到素材到完成一批生成图的平均耗时。

## P0/P1/P2 不足判定
- P0：无法启动、核心图库不可用、数据路径不安全、管理状态不可见、验证无法复现。
- P1：搜索/详情/收藏/生成链路可用但慢、脆弱或难维护。
- P2：缺少体验增强、批量能力、推荐/聚合、发布复盘等非阻断能力。
